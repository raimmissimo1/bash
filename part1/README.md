### BASH

When do we use bash script??
We use bash scripts when we need to automate our daily job
To make bash scripts , firstly you need to write valid commands inside of that bash script file and then you need to do that file executable by chmod +x file-name
